pipeline {
  agent { node { label 'ruby' } }
  stages {
    stage('Setup') {
      steps {
        sh '''
          bundle install
        '''
      }
    }
    stage('RSpec') {
      steps {
        sh 'rake'
      }
    }
  }
}